{% extends '::admin.html.twig' %}

{% block title %}Création d'un utilisateur{% endblock %}
{% set submenu = 'user_new_' ~ country %}

{% block body %}

    <div class="row-fluid">
        <div class="span12">
            <form action="{{ path('LFUserBundle_Edition_new', { 'country': country }) }}" method="post" {{ form_enctype(form) }}>
                {{ form_rest(form) }}
                <button type="submit" class="btn">Enregistrer</button>
            </form>
        </div>
    </div>
{% endblock %}
